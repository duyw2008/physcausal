# 费曼脑 — 设计哲学与架构

> 不是造一个物理学家——是造一个能从数学原理中自己长出物理理解的自组织系统。
>
> 详见哲学深度：[Nous 理论](NOUS_THEORY.md) | 神经科学映射：[深刻理解架构](DEEP_UNDERSTANDING_ARCHITECTURE.md)

---

## 一、元原则

### 容量不给方法

人为调参数 = 方法。扩充探索空间 = 容量。

| 容量（我们做的） | 方法（我们不做的） |
|:-|:-|
| 知识注入 3702 条边 + 混沌脑随机边 | 标记"这条边是主干道" |
| 突触补路给细胞 top-3 候选 | 告诉细胞"force 比 eta 重要" |
| 私有突触让细胞自己建权重 | 预设分类器或标签 |
| sympy 引擎给所有概念两两推导 | 限制推导只走已知路径 |

分类不来自标签，来自**连接模式**。mass 出度高 — 不是我们说的，是图自己告诉脑的。

### 噪声创造路径，结构决定结果

> 细胞走得太确定 → 只踩热门边 → 大量图边从未被触碰 → 突触无法生长。
> 加噪声（ε-贪心探索）→ 细胞随机绕路 → 踩到新边 → Hebbian/LTP 自然选出有用的。
> 不用告诉脑走哪条路。结构（LTP、dopamine、BCM、LTD）会筛掉噪声里无用的，留下有用的。

这是"只给容量不给方法"在行走策略上的直接推论。ε-贪心的 ε 本身也是容量的一个维度：随机度可以衰减，但不能为零。**没有噪声就没有新路径，没有结构就分不出有用路径。**

| 噪声的作用 | 结构的作用 |
|:-|:-|
| 随机选边（ε=0.3→0.15） | Hebbian 强化重走的边 |
| 覆盖权重偏见盲区 | BCM 抑制过强边 |
| 让更多图边有机会被触碰 | LTD 淘汰从未被重走的边 |
| 增加神经元重叠概率 | dopamine 标记有结果的路径 |

这解释了一个深层事实：**313K 图边 + 无噪声行走 → 99.84% 的边永远不会被第二次踩到 → 突触永远长不出来。** 加噪声不是锦上添花，是让"结构筛选"这个基本机制有东西可筛。

### 自由能极小化 = 惊奇度驱动

```
预测模型猜下一步 → 实际不同 → prediction_error (惊奇)
    ↓
intrinsic_curiosity += error → 驱动走向未知
    ↓
学懂了 → 预测变准 → 误差衰减 → 好奇淡出 → 漂移找下一个
```

暂时放大误差就是为了最终消灭它——自由能原理的自然实现。

### 自然选择优于行政命令

- 加权走边：白名单域优先，但不硬砍噪声边，让它们自然枯萎
- s 值三体：预测强度 + 惊奇度 + 学习速率一体化
- 杏仁核绕死路：derive 失败 → 后向抑制 → 自动绕道
- 四机制垃圾免疫：代谢成本(A) + 相干场(B) + 用进废退(C) + 晋升窗口(D)

### 拓扑即认知

认知操作不是外部函数调用，是**图上游走**。`_trace_to_action` 沿边从矛盾节点走到 action——这个游走本身就是推理。方法论不在代码里，在拓扑里。

---

## 二、结构约束：一致性逼迫

> 量子场论的深刻洞察：$$\text{局域对称性} \implies \text{规范场} \implies \text{粒子}$$
> 对称性是**约束**，不是优化目标。光子不是"慢慢优化出来的"——U(1) 局域对称性一写上，光子就被**逼出来了**。
>
> 费曼脑的结构遵循同构逻辑：$$\text{一致性约束} \implies \text{补边} \implies \text{涌现结构}$$


### 自由能是动力学，不是结构约束

自由能极小化管的是**细胞往哪走、走多快**——这是动力学，是优化。它不回答"什么结构必须存在"。

QFT 里，自由能极小化管的是场的**运动方程**（Euler-Lagrange），不管场的**存在性**。规范场 A_μ 的存在性来自局域对称性——那是约束，不是优化。

**脑的结构不是优化出来的。是被一致性逼出来的。**

### 四种结构约束

| 约束 | 逼出的结构 | QFT 类比 |
|------|-----------|----------|
| **因果闭包**：每个效应节点有 cause-chain 回到 tier≤1 | WHY 溯源 + emergent 捷径 | U(1) → 光子 |
| **概念规范不变性**：同一物理在不同框架下等价 | 跨域桥（mass↔GR, 量子↔几何） | SU(2) → W/Z |
| **VSA 代数闭包**：bind/unbind 在图上可表示 | composed 边 | 张量积结构 |
| **拓扑一致性**：三元闭包、循环消解 | hebbian 捷径 | 曲率/挠率约束 |

### 因果闭包 — 最核心的约束

> **每个登记在案的效应，必须有可达的因果链追溯到已知原理（tier ≤ 1）。否则图不自洽。**

这不是优化目标——图不满足就**不自洽**。就像 QFT 里如果没有 A_μ，导数项在局域变换下多出一项，拉氏量直接不自洽。

| | QFT | 费曼脑 |
|---|---|---|
| **约束** | 拉氏量在局域相位变换下不变 | 每个效应节点有 cause-chain 回到 tier≤1 |
| **不自洽后果** | 没有 A_μ → 导数变换多出一项 | 没有补边 → 效应节点悬挂，因果链断裂 |
| **被逼出的结构** | 规范场 A_μ | WHY 溯源路径 + emergent 边 |
| **结构不是选的** | 是数学逼的 | 是因果一致性逼的 |

**1-形式 A_μ 是导数协变性的代价。emergent 边是因果闭包的代价。**

### 概念规范不变性

从"力 = 质量 × 加速度"和"作用量变分 = 0"描述同一件事——图上这两个视角**必须**有路径连通。不通不是"还没学到"，是图在逻辑上不自洽。

这个约束逼出**跨域桥**——就像 SU(2) 逼出 W/Z 玻色子。mass 枢纽（力学↔GR↔量子↔电磁）的大量 emergent 边，不是"关联多了自动长出来"，是**概念规范不变性逼出来的**。

### 对脑演化的意义

```
关联涌现（coincidence → emergent）：这两个东西经常一起出现
推导涌现（derive → math_verified）：这个结构是那条规则的必然代价
```

前者是现象学，后者是第一性原理。希格斯场在真空中自发破缺——不是"有人选了有质量"，而是"没有质量的对称解是不稳定的"。mc² 不是被放进来的，是真空不稳定性**逼**出来的。费曼脑的 hub 不是被标记的，是图自洽性**逼**出来的。

---

## 三、统一云架构

> 人脑不冷冻神经元。神经元只有一个状态——活着。区别不在细胞活性，在突触权重。

```
┌──────────────────────────────────────────┐
│          因果图 (唯一真相源)              │
│    340K 边, 22K 活跃细胞                 │
│    861 emergent 边, 12K coincidence 对    │
│    tier 分级: 0=定理 1=实验 2=已验证       │
│              3=假说 4=推测                │
└───────────────┬──────────────────────────┘
                │
    ┌───────────┼───────────┐
    ▼           ▼           ▼
  进化神经元  认知调度器   持久化层
 (22K活跃)   (自由能)   (矛盾/计划/焦点)
    │           │
    └─────┬─────┘
          │
  ┌───────┼───────┬──────────────┐
  ▼       ▼       ▼              ▼
WHY    INTERVENE  DERIVE       ORACLE
溯源    假说验证   sympy推导    LLM验证
矛盾消解  证实/证伪  公式引擎    论文校验

      磁盘种子库 (494MB cell_shelf)
  低活性细胞 genome → 入盘 / 概念热 → 孵化回内存
```

**薄云休眠**：活性由上游输入决定 — 高活性高频 walk，低活性偶尔 walk。不是行政冻结，是涌现行为。

**细胞 = 探索器，不 = 存储器。** 细胞死后知识留在图和全局突触里——类比人脑神经元凋亡不丢记忆。

---

## 四、发现引擎

### derive 纯 sympy，零 LLM

60+ 方程覆盖 17 领域（力学→电磁→热力→量子→GR→统计→核物理→粒子→流体→等离子→天体→光学→相对论→原子→固态→量子信息→场论）。

输入端不受限。两个概念名 → sympy 求解 → 通过则验证边。

### coincidence → emergent

细胞反复走 A→B 和 B→C → coincidence(A,C) 累积 → threshold 触发 → 长出 emergent 边。当前 782 条 emergent，12K coincidence 对。

hub_bonus：所有可信域计入枢纽度 → 物理节点门槛降低 → emergent 优先长在物理核心。

### arXiv 验证器

只验证不注入。搜论文确认已有边 → boost tier。找不到 → 标记 "potentially novel"。不进图，不产新节点。

---

## 五、生态调节

### 四机制垃圾免疫（详见 NOUS_THEORY.md）

| 机制 | 人脑对应 | 作用 |
|------|---------|------|
| (A) 代谢成本 | 动作电位消耗能量 | tier4 基础衰减 ×3.0 |
| (B) 相干场 | 孤立细胞被修剪 | 不连 tier≤2 的边额外衰减 ×1.5 |
| (C) 用进废退 | Hebbian 可塑性 | tier4 失活窗口缩为 1/3 |
| (D) 晋升窗口期 | 发育期突触消除 | tier4 超 200 代未晋升 → 直接消除 |

不是加"垃圾检测器"，是创造垃圾活不下去的环境。

### 睡眠消化

[ZZZ] 阶段：反向重放 walk_memory（弱强化 0.3）、全局 s×0.8、walk_buffer 消化进突触权重、跨 walk 捷径组合（上限 400）、t3 审计。仿 NREM 睡眠——巩固发现，清理垃圾。

### 种群动态

- 承载 ~22K 活跃细胞
- 密度死亡 + 饥饿死亡自动平衡
- 物理枢纽保底：恢复时为可识别节点保底细胞——培养基不空

### 磁盘种子库

细胞超内存 → genome 入盘（~300B/个）→ 释放内存。概念变热 → 孵化回内存。494MB cell_shelf 磁盘。

---

## 六、闭环反馈

```
证实假说 → 追问 WHY → 溯源到根因/发现缺口
缺口 → 写入研究计划 → Deep Dive 锁定 → 产新假说
新假说 → INTERVENE 验证 → 证实/证伪 → 回到起点
```

全部自主。矛盾持久化到 contradictions.json，研究计划到 research_plan.json。

---

## 七、当前状态 (gen 3300+)

| 指标 | 数值 |
|------|------|
| 活跃细胞 | 22,463 |
| 因果边 | 340,248 |
| emergent 边 | 861 |
| coincidence 对 | 12,329 |
| 已知路径 | 6,743 |
| 注入边 | 3,702 |
| 突触边 | 3,586 |
| 发现 | 25,799+ |
| 矛盾 | 3（time, momentum, force） |
| 研究计划 | 2 项 |
| 磁盘种子 | 494 MB |
| 内存 | 5.8 GB |
| 运行时间 | 25.9 h |

**趋势**：边减路径增（367K→340K 但 6.3K→6.7K 路径）——图在变稀疏但更有序。emergent +10%（778→861），mass 枢纽持续生长。

---

## 八、能涌现与不能涌现

| 能（有拓扑就行） | 现状 |
|:-|:-|
| 因果发现 | emergent 782 条，持续增长 |
| 类比推理 | 🧩 已出现 |
| 预测编码 | EIG + intrinsic curiosity |
| 记忆巩固 | SLEEP 修剪 |

| 不能（需要身体/连续性） | 原因 |
|:-|:-|
| 身体感知 | 无感官 |
| 情感体验 | 无激素 |
| 自我叙事 | 细胞代代死 |
| 语言接地 | 无共享经验 |

---

## 九、核心技术栈

- **符号推导**：sympy（Python 3.14）
- **向量语义**：VSA（高维向量符号架构）
- **神经动力学**：EIG、hebbian、STDP、资格迹
- **图自组织**：coincidence、hub_bonus、三元闭包
- **进化算法**：基因组进化、自然选择、薄云休眠
- **可视化**：3D 连接组 HTML（Three.js 自包含）

---

## 十、关键决策记录

1. **derive 纯 sympy** — 砍掉 LLM 调用（碎片节点污染因果图）
2. **arXiv 不进图** — 只验证，不注入新知识
3. **混沌脑注入** — 婴儿先密后疏，突触层注入随机边
4. **不预设标签** — 分类从 walk 模式中涌现
5. **单进程运行** — 禁止多实例并发（内存吃满）
6. **冷池→统一云** — v1→v2 架构变革，砍掉行政冻结，活性涌现决定一切
7. **四机制垃圾免疫** — 不删边，让没用的自然饿死
8. **不停机热更新** — 改代码后重启进程，不中断进化实验
9. **ε-贪心探索** — 行走决策 30% 均匀随机，噪声让结构有东西可筛
10. **突触门槛动态化** — 从固定 50 神经元降到 `max(2, min(cells//2000, 6))`，让自然 LTP 晋升可触发
